# Copy to .env.telemetry and set your backend WebSocket base URL (no /ws path).
# Robot and dashboard can run on different machines (e.g. ws://your-server:8000).

BACKEND_BASE_URL=ws://localhost:8000
